# GIT GROUP does not call any external AI API — face matching runs fully
# on-device in the browser (@vladmandic/face-api), and data is stored in a
# local SQLite database (data/gitgroup.sqlite). No secrets are required.

# Optional: port for the API server in dev (defaults to 3001).
# API_PORT=3001
